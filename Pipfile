[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
check-manifest = ">=0.37"
matplotlib = ">=3.0.3"
numpy = ">=1.10.0"
pylint = ">=2.3.1"
pytest = ">=4.3"
twine = ">=1.13.0"

[packages]
python-dateutil = ">=2.8.0"

[requires]
python_version = "3.6"
